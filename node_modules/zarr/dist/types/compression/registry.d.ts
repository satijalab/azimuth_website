import type { Codec, CompressorConfig } from 'numcodecs';
interface Options {
    level?: number;
    cname?: string;
    blocksize?: number;
    clevel?: number;
    shuffle?: number;
}
export declare function getCodec<T extends Codec>(config: Options & CompressorConfig): T;
export {};
